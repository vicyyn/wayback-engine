{
  "name": "wayback-engine",
  "version": "0.1.2",
  "description": "Solana Wayback Engine",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "declaration": true,
  "strict": true,
  "outDir": "./lib",
  "scripts": {
    "start": "ts-node index.ts",
    "build": "tsc",
    "test": "mocha --timeout 600000 --reporter spec --require ts-node/register tests/**/*.test.ts",
    "test:balance": "mocha --timeout 600000 --reporter spec --require ts-node/register tests/balance.test.ts",
    "test:balances": "mocha --timeout 600000 --reporter spec --require ts-node/register tests/balances.test.ts",
    "test:tokenbalance": "mocha --timeout 600000 --reporter spec --require ts-node/register tests/tokenbalance.test.ts"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.7.14",
    "nodemon": "^2.0.19",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "@solana/spl-token": "^0.3.4",
    "@solana/web3.js": "^1.56.2",
    "@types/chai": "^4.3.3",
    "@types/mocha": "^9.1.1",
    "axios": "^0.27.2",
    "chai": "^4.3.6",
    "mocha": "^10.0.0"
  },
  "keywords": [
    "Solana",
    "Wayback"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/vicyyn/wayback-engine.git"
  },
  "author": "Vicyyn"
}
